% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hlpr_baseline_kpi.R
\name{hlpr_baseline_kpi}
\alias{hlpr_baseline_kpi}
\title{Compute baseline KPI (response at zero spend) for a channel}
\usage{
hlpr_baseline_kpi(curve_fn, b, c_param, d, e, rc_type)
}
\arguments{
\item{curve_fn}{Response curve function (from [rm_dispatch()]).}

\item{b, c_param, d, e}{Curve parameters (scalars).}

\item{rc_type}{Character string identifying the curve type.}
}
\value{
Scalar: the baseline KPI at zero spend.
}
\description{
For ROI calculations, "incremental KPI" = f(x) - f(0). This helper
computes f(0) for each of the 6 response curve types.
}
\details{
- **Log forms** (log_logistic, weibull, reflected_weibull): `f(0+) = c`
  analytically, so `c_param` is returned directly (avoids `log(0)`).
- **Standard forms** (logistic, gompertz, reflected_gompertz): evaluates
  `curve_fn(0, b, c_param, d, e)` directly.
}
\keyword{internal}
